# dependencies
node_modules/
.pnpm-store/

# Runtime bundles and declarations are committed so Git installs need no build.
# Only intermediate browser modules and TypeScript build metadata are ignored.
lib/client/
*.tsbuildinfo
*.tgz
.package-smoke/

# private working documents (not part of the public distribution)
HANDOFF.md
CONTINUITY.md
docs/marketplace-entry.md

# local env / secrets — never commit API keys
.env
.env.*
!.env.example

# assistant/session working artifacts
.zcode/

# logs and OS noise
*.log
.DS_Store
Thumbs.db
